# Web Text Input Module

Minimal input infrastructure service.

This service retrieves publicly accessible web pages and converts them into standardized plain text representations.

It performs no interpretation, summarization, or judgment of the retrieved content.

Endpoints:
- GET /health
- GET /fetch?url=
- GET /privacy
